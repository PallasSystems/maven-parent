stages:
  - pre
  - triggers
  - build
  - test
  - analyse
  - report

setup:
  stage: pre
  script:
    - curl "https://gitlab.com/api/v4/projects/29403976/repository/files/build_verification.yml/raw?ref=master&private_token=${GITLAB_TOKEN}" > build-verification.yml
  artifacts:
    paths:
      - build-verification.yml

trigger_build:
  stage: triggers
  trigger:
    include: 
      - artifact: build-verification.yml
        job: setup
    strategy: depend
