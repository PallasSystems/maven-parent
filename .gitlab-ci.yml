stages:
 - triggerBuild
 - triggerRelease
 - build
 - test
 - analyse
 - release

trigger_build_verification:
  stage: triggerBuild
  trigger:
    include:
      - project: ravenholm/ci-pipelines
        branch: main
        file: java/maven/build_verification.yml
  only:
    refs:
      - merge_request
      - main

retrieve_release_files:
  stage: .pre
  script:
    - curl https://gitlab.com/crobar-solutions/ravenholm/ci-pipelines/-/raw/main/java/maven/release_on_branch.yml?private_token=$GITLAB_TOKEN > release_on_branch.yml
  artifacts:
    paths:
      - release_on_branch.yml
  only:
    variables:
      - $CI_COMMIT_REF_NAME =~ /^release\/.*$/i

trigger_release:
  stage: triggerRelease
  trigger:
    include: 
      - artifact: release_on_branch.yml
        job: retrieve_release_files
  only:
    variables:
      - $CI_COMMIT_REF_NAME =~ /^release\/.*$/i
